# Karuu Portfolio

An Astro portfolio and blog for Nguyễn Tiến Khang.

## Development

This project uses pnpm 10.14.

```sh
pnpm install
pnpm dev
```

Use `pnpm build` to create a production build and `pnpm preview` to inspect it locally.
