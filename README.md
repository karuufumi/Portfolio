# KARUU

An Astro portfolio for backend systems, cloud platforms, DevOps, and security-focused software.

## Local development

```bash
npm install
npm run dev
```

Create a static production build with `npm run build`. The generated `dist/` folder is ready for Cloudflare Pages.
